AFS_STORAGE_ROOT=storage
AFS_DATABASE_PATH=storage/afs.sqlite
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:8000

# Optional local-only LLM endpoint for later planner replacement.
# Examples: Ollama or LM Studio OpenAI-compatible server.
LOCAL_LLM_BASE_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=local-model
