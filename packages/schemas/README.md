# AFS Schemas

The canonical MVP schemas currently live in `services/orchestrator/app/schemas.py`.
This package is reserved for shared Pydantic models and generated JSON schema
exports as the monorepo matures.
