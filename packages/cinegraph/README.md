# AFS CineGraph

Reserved for reusable world state, scene graph, shot graph, and audio-visual
event graph logic. The MVP implementation starts in the orchestrator so the
end-to-end local pipeline stays simple.
