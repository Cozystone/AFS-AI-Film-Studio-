# AFS Renderer Adapters

Renderer adapters are backend-agnostic. The first implementation is
`MockRenderer`; ComfyUI, LTX, Wan, and FramePack should remain optional adapters
rather than core dependencies.
